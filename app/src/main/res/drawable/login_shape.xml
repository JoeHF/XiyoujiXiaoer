<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <item
        android:top="-2dp">
        <shape>
            <!-- 填充 -->
            <solid
                android:color="@android:color/white"/><!-- 填充的颜色 -->
            <!-- 描边 -->
            <stroke
                android:width="1dp"
                android:color="@color/lightgray" /> <!-- 定义描边的宽度和描边的颜色值 -->
        </shape>
    </item>
</layer-list>